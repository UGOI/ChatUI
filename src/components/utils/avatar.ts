// utils.ts
export function getClientAvatar() {
    // For now, let's return a hardcoded URL
    // Later, you can replace this function with one that fetches the avatar URL for the currently connected client from your desired source
    return "https://placekitten.com/50/51";
}
